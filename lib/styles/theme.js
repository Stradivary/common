/* eslint-disable import/prefer-default-export */
import colors from './colors';
import spacings from './spacings';
import radius from './radius';
import { screenSizes, media } from './breakpoints';
import { responsive } from './mixins';

export const theme = {
  colors,
  spacings,
  radius,
  screenSizes,
  media,
  responsive,
};
